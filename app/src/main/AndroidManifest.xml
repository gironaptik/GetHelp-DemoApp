<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.afeka.gethelp_demoapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VolunteerTable"></activity>
        <activity android:name=".EmptyListRequest" />
        <activity android:name=".AcceptedActivity" />
        <activity android:name=".CancelActivity" />
        <activity android:name=".ListRequestActivity" />
        <activity android:name=".EmergencyScreen" />
        <activity android:name=".SentRequest" />
        <activity android:name=".SendingRequest" />
        <activity android:name=".AdditionActivity" />
        <activity android:name=".WhereActivity" />
        <activity android:name=".WhenActivity" />
        <activity android:name=".RequestForm1Activity" />
        <activity android:name=".NewRequestActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>